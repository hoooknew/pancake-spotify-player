<ctrl:BaseWindow x:Class="miniplayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:miniplayer.ui.controls"
        xmlns:cvt="clr-namespace:miniplayer.ui.converters"
        xmlns:ui="clr-namespace:miniplayer.ui"
        xmlns:mdl="clr-namespace:miniplayer.models"
        xmlns:local="clr-namespace:miniplayer"        
        mc:Ignorable="d"
        SizeToContent="Height"
        Title="MainWindow" Height="80" Width="400"
        BorderThickness="1" BorderBrush="SlateGray"
        MinWidth="250">
    <Window.Resources>
        <cvt:TextBoxsToMaxSizeMultiConverter Padding="8" x:Key="TextBoxsToMaxSizeMultiConverter"/>
        

        <cvt:BoolToGeometeryConverter x:Key="FavoriteConverter" 
                                      TrueValue="{StaticResource geo_favorite}" 
                                      FalseValue="{StaticResource geo_favorite_border}"
                                      NullValue="{StaticResource geo_favorite_border}"/>


        <cvt:BoolToVisibilityConverter x:Key="uiConverter" TrueVisibility="Hidden" FalseVisibility="Visible"/>
        <cvt:BoolToVisibilityConverter x:Key="signinConverter" TrueVisibility="Visible" FalseVisibility="Collapsed"/>

    </Window.Resources>
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static ui:PlayerCommands.SignIn}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
        <CommandBinding Command="{x:Static ui:PlayerCommands.Shuffle}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
        <CommandBinding Command="{x:Static ui:PlayerCommands.SkipPrev}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
        <CommandBinding Command="{x:Static ui:PlayerCommands.PlayPause}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
        <CommandBinding Command="{x:Static ui:PlayerCommands.SkipNext}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
        <CommandBinding Command="{x:Static ui:PlayerCommands.Repeat}" CanExecute="CommandBinding_CanExecute" Executed="CommandBinding_Executed"/>
    </Window.CommandBindings>
    <Grid>
        <ctrl:SignIn Grid.Column="1" Visibility="{Binding NeedToken, Converter={StaticResource signinConverter}, FallbackValue=Collapsed}"/>


        <Grid x:Name="ui_grid" Grid.Column="1" 
              Visibility="{Binding NeedToken, Converter={StaticResource uiConverter}, FallbackValue=Hidden}">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition MinWidth="50" Width="*">
                            <ColumnDefinition.MaxWidth>
                                <MultiBinding Converter="{StaticResource TextBoxsToMaxSizeMultiConverter}">
                                    <MultiBinding.Bindings>
                                        <Binding ElementName="_title_text"/>
                                        <Binding ElementName="_artist_text"/>
                                        <Binding Path="Text" ElementName="_title_text"/>
                                        <Binding Path="Text" ElementName="_artist_text"/>
                                    </MultiBinding.Bindings>
                                </MultiBinding>
                            </ColumnDefinition.MaxWidth>
                        </ColumnDefinition>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel x:Name="_title_artist_text" HorizontalAlignment="Left">
                        <TextBlock x:Name="_title_text" FontWeight="DemiBold" TextTrimming="CharacterEllipsis" Text="{Binding Title}"/>
                        <TextBlock x:Name="_artist_text" Grid.Row="1" Foreground="SlateGray" FontWeight="Bold" FontSize="10" Text="{Binding Artist}"/>
                    </StackPanel>

                    <ctrl:PathButton 
                            x:Name="_favorite_btn"
                            Grid.Column="1" 
                            HorizontalAlignment="Left"
                            Margin="4,0,8,0" Height="30" Width="30"
                            Background="Transparent"
                            BorderThickness="0"
                            PathData="{Binding IsFavorite, Converter={StaticResource FavoriteConverter}}" 
                            PathFill="{StaticResource spotify_green}"
                            Click="_favorite_btn_Click"/>
                </Grid>

                <ctrl:PlayerControls Grid.Column="1" HorizontalAlignment="Center" Margin="0,0,18,0"/>

            </Grid>
            <ctrl:PlayerProgressBar Grid.Row="1" Margin="2,0"/>
        </Grid>
    </Grid>
</ctrl:BaseWindow>