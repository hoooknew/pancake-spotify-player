<ctrl:BaseWindow x:Class="pancake.PlaylistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pancake"
        xmlns:ctrl="clr-namespace:pancake.ui.controls"
        mc:Ignorable="d"
        Title="PlaylistWindow" Height="120" Width="300">
    <Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <ItemsControl/>
            <Image Source="{Binding Playing.Image.Url}"/>
            <ItemsControl ItemsSource="{Binding Queued}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Image Source="{Binding Image.Url}" Width="80"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </StackPanel>
    </Grid>
</ctrl:BaseWindow>
